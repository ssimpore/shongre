# @shongre/features

Shared feature presentation and framework-independent behavior. Route and
navigation composition remain in `frontend/` and `mobile/`; feature rules live
here when the product meaning is the same on every platform.
